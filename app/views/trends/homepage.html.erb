<div class="row">
  <div class="text-center">
    <h1>Trend.ng</h1>
    <p>View Instagram photos of Top 10 Trends on Twitter!</p>
  </div>
</div>

<div class="row">
  <div class="text-center">
    <h1>Cities</h1>
    <% @locations.each do |l| %>
      <p><%= link_to l.name, trends_path(:woeid => l.woeid) %></p>
    <% end %> 
  </div>
</div>

